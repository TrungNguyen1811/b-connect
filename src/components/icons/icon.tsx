import { Book, BookIcon, Home, LaughIcon, LayersIcon, LucideContact, LucideIcon, StoreIcon, Tags } from 'lucide-react'

export const Icons: { [key: string]: LucideIcon } = {
  home: Home,
  book: Book,
  tags: Tags,
  store: StoreIcon,
  logo: BookIcon,
  list: LayersIcon,
  contact: LucideContact,
  policy: LaughIcon,
}
